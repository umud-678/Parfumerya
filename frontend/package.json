{
  "name": "parfumerya-frontend",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,storefront,admin -c yellow,cyan,magenta \"npm run dev --prefix ../api\" \"npm run dev:storefront\" \"npm run dev:admin\"",
    "dev:storefront": "npm run dev --prefix storefront",
    "dev:admin": "npm run dev --prefix admin",
    "stop": "node ../scripts/stop-dev.js",
    "build": "npm run build --prefix storefront && npm run build --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
